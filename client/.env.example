# Base URL of the Ekehi API (no trailing slash).
# Used by client/src/lib/api.ts for all fetch calls.
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Public origin of the deployed site. Used in metadata, share links, etc.
VITE_SITE_URL=http://localhost:3000
